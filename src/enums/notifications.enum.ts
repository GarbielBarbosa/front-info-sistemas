
export enum NotificationType {
    info = 'info',
    success = 'success',
    warning = 'warning',
    danger = 'danger',
}

export enum FromType {
    top = 'top',
    bottom = 'bottom',
}

export enum AlignType {
    center = 'center',
    left = 'left',
    right = 'right',
}